<Window x:Class="Surtitodo.SurtitodoPOS.Applications.Frontends.SyncCenter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:layout="clr-namespace:Surtitodo.SurtitodoPOS.Applications.Frontends.SyncCenter.Views.Controls.Layout"
        Title="Surtitodo Sync Center"
        Width="1450"
        Height="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <layout:Sidebar x:Name="MainSidebar" Grid.Column="0"/>

        <!-- Contenido -->
        <Grid Grid.Column="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <layout:Header
                x:Name="MainHeader"
                Grid.Row="0"
                Title="Document Grouping To SAP"
                Subtitle="Integración de facturas y devoluciones agrupadas con SAP Business One"
                LastUpdate="--"/>

            <!-- Módulo activo -->
            <ContentControl x:Name="ModuleContent"
                            Grid.Row="1"
                            Margin="0,20,0,20"/>

            <!-- Footer -->
            <layout:Footer Grid.Row="2"/>
        </Grid>
    </Grid>
</Window>